# Basic auth – csak staging/beta környezetben. A jelszófájlt a host-ról kell
# bemountolni /etc/nginx/.htpasswd-ra (lásd gateway README).
auth_basic           "Velcord Beta";
auth_basic_user_file /etc/nginx/.htpasswd;
