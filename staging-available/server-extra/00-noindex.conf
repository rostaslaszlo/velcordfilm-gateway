# Ne indexelje semmilyen kereső – csak staging/beta környezetben aktív.
add_header X-Robots-Tag "noindex, nofollow, noarchive" always;

location = /robots.txt {
    add_header Content-Type text/plain always;
    return 200 "User-agent: *\nDisallow: /\n";
}
